# Use root/example as user/password credentials
version: "3.9"
name: system-qa

networks:
  nt-qa:
    name: nt-qa
    driver: bridge
  nt-webserver:
    external: true
  nt-databases:
    external: true
  nt-observability:
    external: true

include:
  - QA/docker-compose.sonarqube.yml
  # - QA/docker-compose.locust.yml
  # - QA/docker-compose.k6.yml
  # - QA/docker-compose.ngrinder.yml
  # - QA/docker-compose.cypress.yml
