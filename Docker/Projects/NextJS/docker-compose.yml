x-default-logging: &logging
  driver: 'json-file'
  options:
    max-size: '5m'
    max-file: '2'
    
networks:
  nt-projects:
    name: nt-projects
    driver: bridge
  nt-databases:
    external: true
  nt-message-broker:
    external: true
  nt-searchengine:
    external: true

services:
  default-nextjs:
    build:
      context: .
      dockerfile: Default-NextJS/Dockerfile
      target: dev_nextjs
    image: node.nextjs:latest
    hostname: default-nextjs
    container_name: default-nextjs
    restart: unless-stopped
    working_dir: /project
    expose:
      - ${DEFAULT_NEXTJS_PORT}
    ports:
      - ${DEFAULT_NEXTJS_HOST_PORT}:${DEFAULT_NEXTJS_PORT}
    networks:
      - nt-private
      - nt-databases
      - nt-message-broker
      - nt-searchengine
    stdin_open: true #docker run -i
    tty: true #docker run -t
    volumes:
      - default-nextjs-volume:/project:rw
      - resource-volume:/resources:rw
    logging: *logging

volumes:
  default-nextjs-volume:
    driver: local
    driver_opts:
      o: bind
      type: none
      device: ${DEFAULT_SOURCECODE_DIR}/default-NextJS
  resource-volume:
    driver: local
    driver_opts:
      o: bind
      type: none
      device: ${DEFAULT_RESOURCES_DIR}/Contents
