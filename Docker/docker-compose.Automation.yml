# Use root/example as user/password credentials
version: "3.9"
name: system-automation

networks:
  nt-automation:
    name: nt-automation
    driver: bridge
  nt-webserver:
    external: true
  nt-qa:
    external: true
  nt-databases:
    external: true
  nt-observability:
    external: true
  nt-message-broker:
    external: true
    
include:
  - Automation/docker-compose.n8n.yml
