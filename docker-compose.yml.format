services:
  sample:
    image: 
    container_name: 
    hostname: 
    restart: unless-stopped
    ports:
      - 
    expose:
      - 
    networks:
      - 
      -        
    labels:
    volumes:
    environment:
      
    logging:
      driver: "json-file"
      options:
        max-size: "5m"
        max-file: "2" 

volumes: